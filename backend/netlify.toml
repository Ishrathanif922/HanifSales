[build]
  command = "npm run build"

[functions]
  directory = "api"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/api/:splat"
  status = 200

[build.environment]
  SECRETS_SCAN_ENABLED = "false"
